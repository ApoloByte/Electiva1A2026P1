// Interface para el objeto Animal (Punto 2)
export interface Animal {
  name: string;
  age: number;
  color: string;
  isPet: boolean;
  height: string; // Puede ser string (ej: "50cm") o number según prefieras
}